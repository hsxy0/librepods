name: Android Debug CI

on:
  push:
    branches:
      - '**'
    paths:
      - 'android/**'
      - '.github/workflows/ci-android.yml'
  pull_request:
    paths:
      - 'android/**'
      - '.github/workflows/ci-android.yml'
  workflow_dispatch:
    inputs:
      branch:
        description: Branch or commit to build
        required: true
        default: main
  workflow_call:

# This fork validates debug builds without upstream release keys or publishing.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || github.ref }}

      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          # Override v3's default: Google no longer serves the legacy "tools" package.
          packages: 'platform-tools'
          log-accepted-android-sdk-licenses: 'false'

      - name: Install project SDK and native build tools
        id: sdk
        # Keep these versions in sync with android/app/build.gradle.kts.
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p android/build
          sdkmanager --list --channel=3 > android/build/ci-sdk-packages.log 2>&1
          sdkmanager --channel=3 \
            "platforms;android-37.0" \
            "ndk;30.0.14904198" \
            "cmake;3.22.1" \
            2>&1 | tee android/build/ci-sdk-install.log
          test -f "$ANDROID_HOME/platforms/android-37.0/android.jar"
          test -f "$ANDROID_HOME/ndk/30.0.14904198/source.properties"
          test -x "$ANDROID_HOME/cmake/3.22.1/bin/cmake"

      - name: Prepare release signing
        id: signing
        shell: bash
        env:
          RELEASE_KEYSTORE_BASE64: ${{ secrets.RELEASE_KEYSTORE_BASE64 }}
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          if [[ -z "${RELEASE_KEYSTORE_BASE64:-}" || -z "${RELEASE_STORE_PASSWORD:-}" || -z "${RELEASE_KEY_ALIAS:-}" || -z "${RELEASE_KEY_PASSWORD:-}" ]]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "Release signing secrets are not configured; skipping signed Release APK."
            exit 0
          fi
          printf '%s' "$RELEASE_KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/librepods-release.jks"
          cat > android/local.properties <<EOF
          sdk.dir=$ANDROID_HOME
          RELEASE_STORE_FILE=$RUNNER_TEMP/librepods-release.jks
          RELEASE_STORE_PASSWORD=$RELEASE_STORE_PASSWORD
          RELEASE_KEY_ALIAS=$RELEASE_KEY_ALIAS
          RELEASE_KEY_PASSWORD=$RELEASE_KEY_PASSWORD
          EOF
          echo "available=true" >> "$GITHUB_OUTPUT"

      - name: Build FOSS debug APK
        id: apk
        working-directory: android
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew
          mkdir -p build
          ./gradlew --no-daemon --stacktrace assembleFossDebug \
            2>&1 | tee build/ci-apk.log

      - name: Build signed FOSS release APK
        id: release
        if: ${{ steps.signing.outputs.available == 'true' }}
        working-directory: android
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon --stacktrace assembleFossRelease \
            2>&1 | tee build/ci-release.log

      - name: Run JVM unit tests
        id: tests
        if: ${{ !cancelled() && steps.apk.outcome == 'success' }}
        working-directory: android
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon --stacktrace testFossDebugUnitTest \
            2>&1 | tee build/ci-tests.log

      - name: Run Android Lint
        id: lint
        if: ${{ !cancelled() && steps.apk.outcome == 'success' }}
        working-directory: android
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon --stacktrace lintFossDebug \
            2>&1 | tee build/ci-lint.log

      - name: Checksum debug APK
        if: ${{ !cancelled() }}
        shell: bash
        run: |
          if [[ -f android/app/build/outputs/apk/foss/debug/app-foss-debug.apk ]]; then
            cd android/app/build/outputs/apk/foss/debug
            sha256sum app-foss-debug.apk > SHA256SUMS
          fi

      - name: Checksum release APK
        if: ${{ !cancelled() && steps.release.outcome == 'success' }}
        shell: bash
        run: |
          cd android/app/build/outputs/apk/foss/release
          sha256sum *.apk > SHA256SUMS

      - name: Upload debug APK
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: apk-foss-debug-${{ github.run_id }}
          path: |
            android/app/build/outputs/apk/foss/debug/*.apk
            android/app/build/outputs/apk/foss/debug/SHA256SUMS
          if-no-files-found: warn
          retention-days: 14

      - name: Upload release APK
        if: ${{ !cancelled() && steps.release.outcome == 'success' }}
        uses: actions/upload-artifact@v4
        with:
          name: apk-foss-release-${{ github.run_id }}
          path: |
            android/app/build/outputs/apk/foss/release/*.apk
            android/app/build/outputs/apk/foss/release/SHA256SUMS
          if-no-files-found: error
          retention-days: 14

      - name: Upload test reports and diagnostics
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: android-ci-reports-${{ github.run_id }}
          path: |
            android/app/build/reports/
            android/app/build/test-results/
            android/build/ci-*.log
          if-no-files-found: warn
          retention-days: 14

      - name: Write build summary
        if: ${{ !cancelled() }}
        env:
          SDK_OUTCOME: ${{ steps.sdk.outcome }}
          APK_OUTCOME: ${{ steps.apk.outcome }}
          TEST_OUTCOME: ${{ steps.tests.outcome }}
          LINT_OUTCOME: ${{ steps.lint.outcome }}
        shell: bash
        run: |
          {
            echo '## LibrePods Android debug validation'
            echo "SDK installation: ${SDK_OUTCOME}"
            echo "APK build: ${APK_OUTCOME}"
            echo "Release signing setup: ${SIGNING_OUTCOME}"
            echo "Signed Release APK: ${RELEASE_OUTCOME}"
            echo "JVM unit tests: ${TEST_OUTCOME}"
            echo "Android Lint: ${LINT_OUTCOME}"
            echo "Checked-out commit: $(git rev-parse HEAD)"
            echo 'All three verification steps are required; no errors are ignored.'
            echo 'Artifacts, when produced: debug APK with SHA-256, unit test reports, lint reports, SDK installation logs, and Gradle logs.'
            echo 'No release signing keys are used. No release or Discord notification is published.'
            echo 'CI does not validate AirPods 5 Bluetooth behavior on real hardware.'
          } >> "$GITHUB_STEP_SUMMARY"
